1.list images
docker images

2.kill unused images
sudo docker images | grep " [months|days|weeks]* ago" | awk '{print $3}' | xargs sudo docker rmi -f

3.kill one images
sudo docker rmi -f 'name'

4.delete all the containers
sudo docker rm $(sudo docker stop $(sudo docker ps -aq))

